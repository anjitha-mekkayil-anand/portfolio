# Portfolio

Personal portfolio site — a static one-pager. Next.js (static export), no external dependencies beyond React, system fonts only.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build   # static output in out/
```

## Deploy

Vercel (auto-detects Next.js). Custom domain mapped in the Vercel dashboard.
